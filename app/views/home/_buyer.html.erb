<div class='container'>
  <table class='table table-hover project-table'>
    <thead class='thead'>
      <tr>
        <th>name</th>
        <th>Monthly Fee</th>
        <th colspan='1'></th>
      </tr>
    </thead>

    <% user.plans.each do |plan|%>
    <tr>
      <td><%=plan.name%></td>
      <td><%=plan.monthly_fee%></td>
      </tr>
    <%end%>
  </table>
